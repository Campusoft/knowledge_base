# AWS Secrets Manager

Es un servicio diseñado específicamente para almacenar, rotar y gestionar credenciales (como usuarios/contraseñas de bases de datos, llaves de API o tokens). Utiliza KMS por debajo para encriptar esos secretos.

# AWS KMS

Llaves criptográficas maestras para cifrar discos (SSE) o datos.
